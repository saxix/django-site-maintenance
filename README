Django Maintenance
==================

An maintenance mode that allows you to put your django project into maintenance mode via command line.

I start writing this because all others implementations of maintenance mode did not allow to alter/update the database
schema.

